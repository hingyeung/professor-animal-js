{
  "name": "animal-genie-js",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.111.0",
    "lodash.filter": "^4.6.0",
    "lodash.map": "^4.6.0",
    "lodash.mapkeys": "^4.6.0",
    "lodash.sortby": "^4.7.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "jshint": "^2.9.5",
    "mocha": "^3.5.0",
    "proxyquire": "^1.8.0",
    "sinon": "^3.2.1"
  },
  "scripts": {
    "jshint": "jshint app/**/*.js",
    "test": "npm run test-unit",
    "test-unit": "mocha -R spec **/*.spec.js",
    "package": "rm -rf dist/ && mkdir dist && cp app/*.js dist/ && cp app/*.json dist/ && cp -r app/data dist/ && cp -r node_modules dist/ && cd dist && zip -r lambda.zip *"
  },
  "author": "",
  "license": "ISC",
  "jshintConfig": {
    "node": true,
    "mocha": true,
    "esversion": 6,
    "expr": true
  }
}
