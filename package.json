{
  "name": "animal-genie-js",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.111.0",
    "lodash": "^4.17.4",
    "q": "^1.5.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "jshint": "^2.9.5",
    "lambda-local": "^1.4.2",
    "mocha": "^3.5.0",
    "proxyquire": "^1.8.0",
    "sinon": "^3.2.1",
    "sinon-promise": "^0.1.3"
  },
  "scripts": {
    "clean": "rm -rf dist/",
    "jshint": "jshint app/**/*.js",
    "test": "npm run test-unit",
    "test-unit": "NODE_ENV=dev mocha -R spec './app/**/*.spec.js'",
    "package": "app/scripts/package.sh",
    "run-local-initial": "cd app && lambda-local -l index.js -h myHandler -E '{\"NODE_ENV\":\"dev\"}' -e data/initial_request.js",
    "run-local-followup-yes": "cd app && lambda-local -l index.js -h myHandler -E '{\"NODE_ENV\":\"dev\"}' -e data/followup_yes_request.js",
    "run-local-followup-no": "cd app && lambda-local -l index.js -h myHandler -E '{\"NODE_ENV\":\"dev\"}' -e data/followup_no_request.js",
    "run-local-followup-repeat": "cd app && lambda-local -l index.js -h myHandler -E '{\"NODE_ENV\":\"dev\"}' -e data/followup_repeat_request.js",
    "upload-package-to-lambda": "app/scripts/upload_package_to_lambda.sh"
  },
  "author": "",
  "license": "ISC",
  "jshintConfig": {
    "node": true,
    "mocha": true,
    "esversion": 6,
    "expr": true
  }
}
